{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch game",
            "type": "blazorwasm",
            "request": "launch",
            "cwd": "${workspaceFolder}/Blazeroids.Web"
        }
    ]
}
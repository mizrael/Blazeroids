@inherits BaseAsset<Core.Assets.SpriteSheet>

@if(this.Source is not null){
    <img name="@Source.ElementRef.Id" @onload="@OnLoaded"
        @ref="Source.ElementRef" src="@Source.ImagePath" hidden />
}